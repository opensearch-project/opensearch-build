pipeline {
    agent none
    stages {
        stage('assemble from archive') {
            steps {
                script {
                    archiveAssembleUpload(
                        dryRun: true,
                        platform: 'linux',
                        architecture: 'x64'
                    )
                }
            }
        }
    }
}
