pipeline {
    agent none
    stages {
        stage('sign') {
            steps {
                script {
                    signArtifacts(
                            artifactPath: artifactPath,
                            signatureType: SIGNATURE_TYPE,
                            distributionPlatform: DISTRIBUTION_PLATFORM
                    )
                }
            }
        }
    }
}
