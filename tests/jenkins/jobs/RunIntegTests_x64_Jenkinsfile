pipeline {
    agent none
    stages {
        stage('run integ tests') {
            steps {
                script {
                    runIntegTests(
                        manifest: 'tests/jenkins/data/opensearch-1.3.0.yml',
                        architecture: 'x64'
                    )
                }
            }
        }
    }
}
