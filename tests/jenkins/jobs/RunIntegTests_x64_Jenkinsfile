pipeline {
    agent none
    stages {
        stage('run integ tests') {
            steps {
                script {
                    runIntegTests(
                        testManifest: 'tests/jenkins/data/opensearch-1.3.0-test.yml',
                        buildManifestUrl: 'dummy-build-manifest-url',
                        agentLabel: 'Jenkins-Agent-al2-x64-c54xlarge-Docker-Host'
                    )
                }
            }
        }
    }
}