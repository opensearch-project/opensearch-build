pipeline {
    agent none
    stages {
        stage('notify') {
            steps {
                script {
                    try {
                        buildFailureMessage()
                        currentBuild.result = 'SUCCESS'
                    } catch (Exception err) {
                        currentBuild.result = 'SUCCESS'
                    }
                }
            }
        }
    }
}

