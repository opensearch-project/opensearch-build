---
name: github-actions

on:
  pull_request:
    paths:
      - '.github/actions/find-triggering-pr/**'
      - '.github/actions/discerning-merger/**'
      - '.github/workflows/github-actions.yml'
  push:
    branches:
      - main
    paths:
      - '.github/actions/find-triggering-pr/**'
      - '.github/actions/discerning-merger/**'
      - '.github/workflows/github-actions.yml'

permissions:
  contents: read

jobs:
  test:
    name: Test ${{ matrix.action }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        action:
          - find-triggering-pr
          - discerning-merger
    defaults:
      run:
        working-directory: .github/actions/${{ matrix.action }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: .github/actions/${{ matrix.action }}/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Verify generated action is current
        run: git diff --exit-code -- dist
